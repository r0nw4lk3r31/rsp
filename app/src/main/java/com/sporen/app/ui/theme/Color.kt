package com.sporen.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Sporen brand palette (from sporen.be) ──────────────────────────────
val SporenTeal        = Color(0xFF19AFB1)   // primary — dominant teal-green
val SporenTealDark    = Color(0xFF025751)   // dark teal — headers / active states
val SporenOrange      = Color(0xFFF09743)   // accent / highlight
val SporenBackground  = Color(0xFFFFFFFF)   // white
val SporenSurface     = Color(0xFFF5F5F5)   // light grey card surface
val SporenOnSurface   = Color(0xFF3A3A3A)   // near-black body text
val SporenOnSurfaceMuted = Color(0xFF777777) // secondary text
val SporenOnPrimary   = Color(0xFFFFFFFF)   // white on teal

// Shift accent colors — adapted to the light theme
val ShiftDag    = SporenTeal
val ShiftNacht  = SporenTealDark
val ShiftDp     = SporenOrange
val ShiftManual = Color(0xFF888888)

